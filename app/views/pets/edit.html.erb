<h1>Edita una mascota</h1>

<%= form_tag pet_path(@pet), method: "patch" do %>
  <%= text_field :pet, :name %>
  <%= submit_tag("Crear mascota") %>
<% end %>

<%= form_tag "/pets/#{@pet.id}", method: "patch" do %>
  <%= text_field :pet, :name %>
  <%= submit_tag("Crear mascota") %>
<% end %>
